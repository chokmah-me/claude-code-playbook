---
# global.md — loads every session, keep tiny
# Rules here have no paths: frontmatter, so they load unconditionally.
# Keep this file under 100 tokens.
---

All commits require passing type-check, tests, and lint.
Never commit secrets or long-lived tokens to version control.
REFACTOR_PROGRESS.md must be updated before ending a multi-session task.
